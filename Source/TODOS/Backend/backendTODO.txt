enfore uniqueness of user id in firestore rules
in models, there should be save and delete functions in the object classes... see old CSE 414, should also throw exceptions

Create a db.python file for initializing database stuff


from_dict and to_dict should be reversible...
e.g. create object, call to_dict, create object from_dict, the original and new object should be the same

Create a list of common naming conventions in python:
function naming: snake_case

underscore in front for internal methods (not an endpoint)


add the very first receipt

also think about categories

automate changes, e.g. a user edits the receipt, then the total for the month must also change
or changes the category, then the total for that category must also change

Cloud function
